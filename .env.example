POSTGRES_PRISMA_URL=postgresql://postgres:1234@localhost
POSTGRES_URL_NON_POOLING=postgresql://postgres:1234@localhost

NEXT_PUBLIC_DEFAULT_CURRENCY_CODE=SEK

# Public base URL (used for metadata, OG images etc.)
NEXT_PUBLIC_BASE_URL=https://gemensams.krut.it

# Shared-password gate
APP_PASSWORD=change-me
APP_SECRET=generate-a-long-random-string

# ---- Phase 2b: AI receipt scanning + category extraction (Claude) ----
# To enable: set the key AND flip the relevant flag(s) to true.
# Receipt scanning additionally requires object storage (S3_UPLOAD_* vars) for images.
# ANTHROPIC_API_KEY=sk-ant-...          # your Anthropic API key
# NEXT_PUBLIC_ENABLE_RECEIPT_EXTRACT=false
# NEXT_PUBLIC_ENABLE_CATEGORY_EXTRACT=false